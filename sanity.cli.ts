import { defineCliConfig } from 'sanity/cli'

export default defineCliConfig({
  api: {
    projectId: 'q1u033fj',
    dataset: 'production',
  },
  studioHost: 'jakeportfolio',
  autoUpdates: true,
})
