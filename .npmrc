legacy-peer-deps=true
strict-peer-dependencies=false
# The Vercel project sets NODE_ENV in every environment, which makes npm omit
# devDependencies at install time. The build needs tailwindcss, postcss and
# typescript from devDependencies, so install them regardless.
include=dev
